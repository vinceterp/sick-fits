import Link from 'next/link';

//Link is bound to React pushState to aid with Routing

const sell = (props) => {
    return (
        <div>
            <h1>Sell!</h1>
        </div>
    );
}

export default sell;